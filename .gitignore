# Xcode
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist
*.xcworkspace/xcshareddata/swiftpm/
DerivedData/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

# Build
build/
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods
Pods/
*.xcworkspace
!*.xcodeproj/*.xcworkspace

# Carthage
Carthage/Build/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Xcode Previews
PreviewContent/

# Credentials / Secrets
*.pem
*.p12
*.mobileprovision

# Local environment files (release credentials, paths, etc.)
.env
.env.local

# Export
ExportOptions.plist

# Local distribution / release tooling — kept private (contains personal
# Apple Team ID, notary profile name, signed appcast). Each maintainer keeps
# their own copy locally; the appcast.xml is uploaded as a GitHub Release
# asset, not committed to the repo.
Distribution/
